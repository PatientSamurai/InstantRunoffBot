{
  "name": "instant-runoff-bot",
  "version": "0.1.0",
  "description": "Bot for running instant runoff voting elections in a Discord channel.",
  "main": "dist/index.js",
  "author": "Matt VanderKolk",
  "repository": {
    "type": "git",
    "url": "https://github.com/PatientSamurai/InstantRunoffBot.git"
  },
  "license": "MIT",
  "scripts": {
    "start": "tsc && node .",
    "test": "npm run build && nodemon --inspect",
    "build": "tsc && tslint -c tslint.json -p tsconfig.json"
  },
  "dependencies": {
    "@types/node": "^12.12.31",
    "@types/ws": "^7.2.3",
    "discord.js": "^12.0.2",
    "typescript": "^3.8.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.2",
    "tslint": "^6.1.0"
  }
}
